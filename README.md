# Cypress Automation Project

This project contains automated tests built using **Cypress**.

## 📦 Requirements

Before running the tests, make sure you have the following installed:

- Node.js
- npm

## 🔌 Required Dependency

This project uses XPath selectors. To enable XPath support in Cypress, install the following dependency:

```bash
npm install -D cypress-xpath
